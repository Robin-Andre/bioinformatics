#pragma once
//#include<vector>
#include <fstream>
#include "../Metric.hpp"
//#include <iostream>
//#include "../RFDistance.hpp"
//#include "CommandLineOptions.hpp"
class Converter {

public:

/*static void raxmlToJSON(const std::string& inpath, const std::string& outpath, const std::string& mode_name, size_t tip_count) {
  io::IOData data = RAXMLReader::read(inpath);
  data.mode = mode_name;
  if(mode_name == "ABSOLUTE") {
    data.mean_dst *= (2*(tip_count-3));
  }
  JSONWriter::write(outpath, data);
}

static void matrixToJSON(const std::string& inpath, const std::string& outpath, const std::string& metric_name, const std::string& mode_name) {
  io::IOData data = MatrixReader::read(inpath);
  data.metric = metric_name;
  data.mode = mode_name;
  JSONWriter::write(outpath, data);
}*/


};
